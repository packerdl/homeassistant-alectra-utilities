name: "Alectra Utilities Scraper"
version: "1.0.0"
slug: alectra_scraper
description: "Playwright sidecar that scrapes electricity usage data from the Alectra Green Button portal"
arch:
  - amd64
startup: application
boot: auto
ports:
  8080/tcp: null
ports_description:
  8080/tcp: "Sidecar API (internal only)"
map:
  - addon_config:rw
options:
  sidecar_token: ""
schema:
  sidecar_token: "str?"
